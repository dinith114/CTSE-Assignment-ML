pytest==8.4.2
langgraph==0.0.20
pydantic==2.13.3

# Core dependencies for Member 4's component
requests>=2.31.0
# No other heavy dependencies needed for distance calculation

# For full project (shared across team)
# Ollama runtime is installed separately on the machine (CLI), not via pip.
fastapi>=0.100.0
uvicorn>=0.22.0